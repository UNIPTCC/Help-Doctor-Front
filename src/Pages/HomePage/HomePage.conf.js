export const mapStateToProps = (store) => {
  return {
    store: store // Realize o mapeamento da store do redux para as props aqui
  }
}

export const mapDispatchToProps = (dispatch) => {
  return {} // Realize o mapeamento dos dispatchers do Redux para props aqui
}
