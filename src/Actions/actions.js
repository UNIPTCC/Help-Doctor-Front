import * as types from './actionTypes'
export const SetError = (message = 'Erro') => ({type: types.SHOW_ERROR, message: message})
export const reducer = () => ({ type: types.ACTIOND })
export const actionTest = () => ({ type: types.ACTIONTEST })
