import { @actionName } from '../actions'
import * as types from '../actionTypes'
import assert from 'assert'

describe('@actionName', () => {
  it('Teste básico de uma ação', () => { // Substitua esta linha pelo título do seu teste
    assert.deepEqual(@actionName(), {
      type: types.@actionType
    })
  })
})